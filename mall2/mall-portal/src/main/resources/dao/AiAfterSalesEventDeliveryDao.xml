<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.portal.dao.AiAfterSalesEventDeliveryDao">
    <insert id="insertIgnore" parameterType="com.macro.mall.portal.domain.AiAfterSalesEventDelivery"
            useGeneratedKeys="true" keyProperty="id">
        insert ignore into ai_after_sales_event_delivery (
            event_id, application_id, application_source, event_type, delivery_status, delivered_at, create_time
        ) values (
            #{eventId}, #{applicationId}, #{applicationSource}, #{eventType}, #{deliveryStatus}, #{deliveredAt}, now()
        )
    </insert>
</mapper>
