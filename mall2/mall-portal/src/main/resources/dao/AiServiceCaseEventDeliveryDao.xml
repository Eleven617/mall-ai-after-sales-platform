<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.portal.dao.AiServiceCaseEventDeliveryDao">
    <insert id="insertIgnore" parameterType="com.macro.mall.portal.domain.AiServiceCaseEventDelivery">
        insert ignore into ai_service_case_event_delivery (event_id, case_id, delivery_status, received_at)
        values (#{eventId}, #{caseId}, #{deliveryStatus}, #{receivedAt})
    </insert>
</mapper>
