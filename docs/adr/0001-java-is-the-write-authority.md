# ADR-0001：Java 是交易写入权威

**状态：接受。**

Java 已拥有 JWT、会员归属、订单/商品、资格、售后状态机、MySQL 事务和唯一约束。因此 FastAPI 只保存有 TTL 且与用户/会话绑定的待确认 proposal/action，并在客户明确确认后调用 Java。Java 必须再次校验用户、目标、版本和幂等键；模型和浏览器都没有写库权限。

这样会增加一次服务间调用，但避免将交易正确性建立在 Prompt、前端字段或模型输出上。
