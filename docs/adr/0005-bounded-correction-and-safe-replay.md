# ADR-0005：受限校正与安全回放

**状态：接受。**

结构化输出、工具参数或证据合同失败时，任何业务写入前最多允许一次受限校正。第二次失败、模型不可用或依赖失败都进入安全停止，不进入无限 Reflection 循环。

质量回放使用版本化合成 EvalCase、模拟工具结果和脱敏 RunManifest；不读取真实客户聊天、Token、订单号、RAG 原文或生产 Trace。确定性比较器裁决硬失败，LLM 只能在失败后给出不可自动执行的归因建议。
