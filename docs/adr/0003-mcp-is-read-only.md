# ADR-0003：MCP 仅提供只读工具

**状态：接受。**

MCP 的 `initialize`、`tools/list` 和 `tools/call` 只暴露经服务端身份范围注入的事实读取能力。它不提供创建、取消、修改、退款、SQL、Shell 或任意 HTTP 代理工具。写操作继续走客户确认、FastAPI 受控边界与 Java 复核。

此取舍牺牲了“模型能做一切”的演示效果，换取可验证的最小权限和可拒绝的越权路径。
