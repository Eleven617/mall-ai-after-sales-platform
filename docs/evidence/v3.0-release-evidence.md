# Mall v3.0 发布证据

本文只收录可复跑的本机/合成验证，不把本地结果写成生产 SLA、真实用户准确率、线上吞吐或外部履约接入。与本文同目录的 `v3.0-baseline-manifest.json` 记录被验证源码的 commit、环境和 fixture 摘要。

## 当前 HEAD 补测（2026-09-05，代码提交 `38cf3809e48ec08bead6accc07a4ace27ebf5f59`）

当前 HEAD 的完整分层结果见 [`v3.0-current-head-evidence.md`](v3.0-current-head-evidence.md) 与同名 JSON。确定性门禁、Docker 健康和 Java 定向测试已通过；开放任务真实模型合成评测与 Grounding 结果保留了实际失败，完整浏览器/Java-MySQL/故障恢复 manifest 尚未逐条现场执行。

- FastAPI：`349 passed`；v3 preflight `478/478`、代表性 Runtime `8/8`。
- Quality Agent：`17/17`；任务编排 `11/11`；Chunk/Metadata `8/8`；RAG verifier `36/36`。
- RAG 检索：Dense、Hybrid、Hybrid+Rerank 各 `52/52`；Dense 继续默认。
- 开放任务 Agent：24 个案例 × 3 次 = `24 passed / 48 failed / 0 environment_blocked`。
- Grounding：15 条 = `11 passed / 4 quality_failed / 0 environment_blocked`。
- Docker：8/8 healthy，Compose endpoint `3/3`；Java portal/admin `14/14`、`6/6`；Web build 通过。
- 远程 Actions：本补测提交尚未推送/完成远程运行，不能沿用历史 Actions 链接作为当前 HEAD 证据。

## 历史已验证提交门禁（2026-09-04，`df67753bb923434c0b5d11e448e778cb413b7840`）

- `mall-ci`：[33868598584](https://github.com/Eleven617/mall-ai-after-sales-platform/actions/runs/33868598584)，真实 success。
- `quality-evaluation`：[33868598567](https://github.com/Eleven617/mall-ai-after-sales-platform/actions/runs/33868598567)，真实 success。
- 本机 live-synthetic：36 条 × 3 次，`108/108 passed`，p95 `1438 ms`；它不是生产模型准确率或 SLA。
- 浏览器 E2E 与 Java/MySQL manifest 仍需逐条真实现场运行，不能由 manifest 校验或上述定向测试代替。

## 当前门禁

| 层 | 门禁 | 范围 |
| --- | --- | --- |
| FastAPI | compile、collect、全量 pytest、v3 deterministic preflight | 无真实模型、无业务写入 |
| Runtime release | 478 条唯一合成 Case + 8 条真实运行时安全冒烟 | manifest/fixture hash 受校验 |
| RAG | 现有 52 条版本化黄金集及 Chunk/Metadata 合同 | Dense 为当前默认；Hybrid/Rerank 仅显式实验 |
| Java | portal/admin 定向测试或 CI 等价命令 | Java 仍是事实、权限、事务和最终写入权威 |
| Web | `npm run build` | 只构建公开 DTO 页面 |
| Compose | `docker compose --env-file .env.example config --quiet` | 静态合同；真实启动另行标注 |

## v3.0 Runtime 证据

- 发布清单：`evals/v3/release-manifest.json`。
- 校验命令：`python scripts/validate_v3_release_manifest.py --json`。
- 确定性运行命令：`python scripts/run_v3_release_preflight.py --json`。
- 每条 Case 具有唯一 `caseId`、fixture hash、可执行断言、安全摘要和预算；manifest 禁止 `skip`/`allowFailure` 开关。
- 代表性运行覆盖未知 Skill、非法参数、并行预算、模型不可用、确认前置、owner 隔离和公开投影边界。

## 未验证/不宣传

- `manual_live_synthetic`、浏览器 E2E、完整 Compose 跨服务回放需显式启动并单独记录；默认 CI 不调用付费真实模型。
- 未接入真实支付、仓储、物流、维修或生产告警/SLA。
- RAG 指标只适用于当前本地政策语料、模型和机器；不代表生产准确率或成本。
