# v3.0.4 Showcase Supplement Reconciliation

The immutable supplement batch `mall-v3.0.4-showcase-supplement-20260922-b` remains `FAILED/runner_exception` because its wrapper failed while serializing the final summary after both business scenarios and screenshots had completed. The original Report, Ledger and Lock are unchanged.

The derived reconciliation confirms `clarify_pause_resume` (4 steps) and `fact_change_replan` (5 steps), 14 Provider requests, 14 successful HTTP attempts, 48,862 tokens, 14/14 reservations and settlements, and zero unresolved reservations or budget denials. Eight PNGs are archived under `docs/assets/showcase-live-supplement-v304/` with hashes in the accompanying JSON. These are real online supplement assets, not a replacement for the failed original report and not a complete model-evaluation result.
