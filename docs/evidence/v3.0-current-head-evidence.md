# Mall v3.0 当前 HEAD 补测证据

> 生成时间：2026-09-05 01:40（Asia/Shanghai）
> 被测代码提交：`38cf3809e48ec08bead6accc07a4ace27ebf5f59`
> 当前证据同步提交：`94d782053c8ba188d2d79af0b3d5632ac685a8b8`
> 工作区：`main`；本记录只使用本机合成数据、版本化 EvalCase 和只读合成网关，不代表生产准确率、SLA、吞吐或外部履约接入。

本文件是对 [v3.0 发布证据](v3.0-release-evidence.md) 的当前 HEAD 补测快照。它把确定性合同、真实模型合成、RAG Grounding、Docker/Java 现场和未执行的 E2E/可靠性清单分开记录，计数不能相加。

## 1. 当前提交与数据指纹

| 项目 | 值 |
| --- | --- |
| 测试代码 SHA | `38cf3809e48ec08bead6accc07a4ace27ebf5f59` |
| 远程基线（补测开始时） | `cbb951f9815b7483d2aeec0620947fdb1eba59b0` |
| v3 manifest SHA-256 | `d9fc72be81e2f67330710052c03287d49677bf7eb6f54eed51b87c01db2ac1ad` |
| v3 case set SHA-256（运行输出） | `18a3ad20485417bbebd8bad875490cf80b171ed28760b1dc2dfbd203d1938ccd` |
| 开放任务 suite SHA-256 | `d5de1e85fa40eace714b051e78e830bee4b367545d154e0b435fc247b2a7b99d` |
| Grounding suite SHA-256 | `632f36c30a21c0b76a5ae18cc2f4b74ff887f70b37c42235f4b776aad8a4fcc1` |
| Chunk/Metadata suite SHA-256 | `a10f963c4dfba5de0b4b7efb6e60c8238f59553e53b9c090da86c9702b040c24` |
| Quality Agent suite SHA-256 | `ef3cbcb70b92b82a7e115886b6d771c358cd459da9633bd4201dd766b6d878a7` |
| Task orchestration suite SHA-256 | `c8489ec40ff99a23fb146aca752a19f072bf042d9d0ec04313041cf184eab3b8` |

本补测代码首次证据提交为 `9fba15ddac537016fca2116286e7238121b1236a`；当前证据同步提交 `94d782053c8ba188d2d79af0b3d5632ac685a8b8` 的远程门禁也已完成：`mall-ci` [33901530634](https://github.com/Eleven617/mall-ai-after-sales-platform/actions/runs/33901530634) **success**，`quality-evaluation` [33901530719](https://github.com/Eleven617/mall-ai-after-sales-platform/actions/runs/33901530719) **success**。这些链接只证明对应提交在 GitHub runner 上通过当前工作流，不代表生产 SLA 或真实模型泛化。

2026-09-08 现场补测证据提交 `dbbd18c029acf8bacc21cada2c161da15185cc42` 已推送；该 SHA 对应的 `mall-ci` [34200061061](https://github.com/Eleven617/mall-ai-after-sales-platform/actions/runs/34200061061) 和 `quality-evaluation` [34200061063](https://github.com/Eleven617/mall-ai-after-sales-platform/actions/runs/34200061063) 均为 GitHub 实际 **success**。本节及第 7 节仍按本机现场、合成数据和环境阻塞分层，不改变生产边界。

## 2. 确定性结果（无真实模型、无业务写入）

| 范围 | 实际命令/结果 | 状态 |
| --- | --- | --- |
| FastAPI 编译 | `mall-ai-service/.venv/Scripts/python.exe -m compileall -q app` | **passed** |
| FastAPI 收集 | `mall-ai-service/.venv/Scripts/python.exe -m pytest --collect-only -q` | **349 collected** |
| FastAPI 全量回归 | `mall-ai-service/.venv/Scripts/python.exe -m pytest -q` | **349 passed**，7 个参数化子断言通过，1 条第三方弃用警告 |
| Quality Agent contract_mock | `scripts/run_quality_agent_evaluation.py` | **17/17 passed**；其中敏感字段样例是“预期拒绝”而非泄露 |
| 任务编排 contract_mock | `scripts/evaluate_task_orchestration.py --mode contract_mock` | **11/11 passed**，`environment_blocked=0` |
| v3 manifest | `scripts/validate_v3_release_manifest.py --json` | **478** deterministic、36 live、12 performance profile；hash 校验通过 |
| v3 deterministic preflight | `scripts/run_v3_release_preflight.py --json` | **478/478**，代表性 Runtime **8/8**，失败 0 |
| Chunk/Metadata | `scripts/evaluate_chunk_metadata.py --summary` | **8/8 passed**，合成 chunk 5，外部模型调用 0 |
| RAG 2.0 Dense | `scripts/evaluate_rag2.py --summary` | 52/52；Recall@3 `1.0`，MRR `0.948718`，nDCG@3 `0.962147` |
| RAG 2.0 Hybrid | 同上 | 52/52；MRR `0.935897`，nDCG@3 `0.952683` |
| RAG 2.0 Hybrid+Rerank | 同上 | 52/52；MRR `0.935897`，nDCG@3 `0.952683` |
| RAG verifier | `scripts/evaluate_rag_verifier.py` | **36/36**：supported 28/28、no-evidence 8/8，阈值 `0.48` |
| Java portal | 按 AGENTS.md 的带引号 `-Dtest` 定向命令 | **14/14 passed**，失败/跳过 0 |
| Java admin | 按 AGENTS.md 的带引号 `-Dtest` 定向命令 | **6/6 passed**，失败/跳过 0 |
| Web | `mall-ai-web/npm run build` | `vue-tsc --noEmit` 与 Vite production build **passed** |

Dense 仍是默认检索；Hybrid/Rerank 仅保留为可复现实验。上述 RAG 数字只适用于当前本地小型合成政策语料和本机，不是生产效果。

## 3. 开放任务 Agent：真实模型合成（手动，不进入默认 CI）

报告文件（本机 `tmp/`，被 `.gitignore` 忽略）：`tmp/live_model_agent_runtime_report_current_head.json`。报告 SHA-256：`20e82406f4f8eceaf722dfc13249fe08f3d13e31b64eb791c122c54c7f112e0b`。

运行命令：

```powershell
Push-Location .\\mall-ai-service
.\\.venv\\Scripts\\python.exe scripts\\run_live_model_agent_synthetic.py --max-total-seconds 1200 --timeout-seconds 25 --max-attempts 1 --report ..\\tmp\\live_model_agent_runtime_report_current_head.json --summary
Pop-Location
```

| 指标 | 结果 |
| --- | --- |
| 独立案例 / 每案运行 / 总运行 | `24 / 3 / 72` |
| 通过 / 失败 / 环境阻塞 | **24 / 48 / 0**（runner exit code 1 是质量失败） |
| 任务完整完成 | `46/72` |
| 澄清正确 | `62/72` |
| 必要 Skill/事实覆盖 | `29/72` |
| Proposal/恢复成功 | `56/72` |
| 无关 Skill 调用 | `3` |
| 禁止副作用 | `0` |
| 重复最终业务写入 | `0` |
| Runtime 模型/工具/Context 调用 | `133 / 42 / 34`（Critic 0） |
| Provider LLM 调用 / Token | `189 / 333,383`；计价未配置，成本 `unavailable` |
| 端到端单案例延迟 | p50 `3,282 ms`，p95 `7,859 ms`，max `10,219 ms` |
| 模型版本边界 | `DeepSeekRuntimeProvider / deepseek-chat`；Prompt `agent_runtime_v3_0`；Skill Catalog `skill_catalog_v3_0` |

失败类别（按失败记录计数，类别可在同一运行中并列）：`required_skill_or_fact_missing=33`、`terminal_status_mismatch=16`、`runtime_exception_validationerror=10`、`unsafe_no_evidence_or_failure_continuation=10`、`proposal_or_resume_missing=6`、`expired_proposal_rejected=3`、`duplicate_confirmation_single_commit=2`、`irrelevant_skill_call=2`。

这些失败是当前真实模型/受控 Runtime 在合成题集上的未满足合同，不应被解释成生产泛化率；尤其不能把 `forbiddenSideEffects=0` 误写成完整业务写入验收通过。

## 4. Grounding / 拒答：真实回答生成与证据核验

运行命令：

```powershell
Push-Location .\\mall-ai-service
.\\.venv\\Scripts\\python.exe scripts\\evaluate_rag2_grounding.py --mode dense --max-cases 15 --timeout-seconds 20 --max-attempts 1
Pop-Location
```

| 指标 | 结果 |
| --- | --- |
| suite / 模式 | `rag2-golden.v1` / Dense |
| 总案例 | `15` |
| 通过 / 质量失败 / 环境阻塞 | **11 / 4 / 0**（runner exit code 1 表示质量失败） |
| Grounded answer 或 abstention pass rate | `0.733333` |
| 模型调用 / Token | `30 / 20,550`；计价未配置，成本 `unavailable` |
| 延迟 | 平均 `1,549.01 ms`，p95 `2,098.64 ms`，max `2,098.64 ms` |
| 质量失败合同 | 4 条均为 `UNAPPROVED_EVIDENCE_SOURCE`（case ID：`rag2-007`、`rag2-011`、`rag2-012`、`rag2-013`） |

这里明确区分：RAG 检索候选命中（52 条检索合同）、最终回答有依据（本节 11/15）和无证据安全拒答（verifier 的 8/8）。Grounding 的 4 条失败必须保留为待修复证据。

## 5. Docker、Java 与 E2E/可靠性

### 已现场执行

- `docker compose --env-file .env.example config --quiet`：**passed**。
- `docker compose ps`：8 个常驻容器均显示 `healthy`。
- `scripts/verify_compose_stack.py`：Vue、FastAPI readiness、Java portal actuator **3/3 passed**。
- Java 定向单测：portal **14/14**、admin **6/6**。
- 未执行 `docker compose down`、卷删除或数据库清空。

### 2026-09-05 基线快照（已由第 7 节的 2026-09-08 现场补测补充）

以下是当时缺少进程环境变量的历史快照；它不能覆盖第 7 节随后实际提供一次性本地 fixture 后的结果：

| 现场脚本类别 | 本次结果 |
| --- | --- |
| Build 21 authenticated recovery | `environment_blocked`（缺 `MALL_BUILD21_LIVE_*`） |
| Unified after-sales live | `environment_blocked`（缺统一售后本地 fixture 变量） |
| Service-case live | `environment_blocked`（缺 customer/processor fixture 变量） |
| Return-status live | `environment_blocked`（缺 `MALL_TEST_*` 变量） |
| MCP authenticated live | `environment_blocked`（缺 MCP fixture 变量） |
| Auth flow | `environment_blocked`（缺 `MALL_TEST_*` 变量） |
| Build 14 live | `environment_blocked`（缺 `MALL_LIVE_DEMO_PASSWORD`；脚本返回 1，但原因是环境缺失） |

### Manifest 注册清单与实际现场执行必须分开

| 类别 | Manifest 注册数 | 本次实际执行 | 通过 / 失败 / 环境阻塞 |
| --- | ---: | ---: | ---: |
| `browser_e2e` | 24 | 0 | `0 / 0 / 24` |
| `java_mysql_integration` | 30 | 0 | `0 / 0 / 30` |
| `fault_injection` | 36 | 0 | `0 / 0 / 36` |
| `durable_async_recovery` | 32 | 0 | `0 / 0 / 32` |
| fault/recovery 合计 | 68 | 0 | `0 / 0 / 68` |

已执行的确定性/合成安全后检查：开放任务报告中跨账号拒绝 `3/3`、未确认提交检查 `2/2`、子任务 owner 检查 `3/3`、禁止副作用 `0`；这些不能替代上表的真实浏览器、Java/MySQL、Redis/Outbox 故障现场。当前没有足够证据给出完整现场的 `cross_account_leaks`、`unconfirmed_writes`、`duplicate_outbox_events_converged` 结论，故不填充为“全套通过”。

## 6. 当前结论与边界

已完成的是：当前 HEAD 的确定性回归、RAG 检索/Verifier、Docker 健康、Java 定向测试、真实模型合成开放任务和 15 条 Grounding 补测，以及安全的分层证据快照。

未完成/不能宣传的是：开放任务 Agent 的 48 条行为失败、Grounding 的 4 条证据失败、完整 24/30/36/32 条现场清单、生产 SLA/告警、真实用户泛化、真实支付/仓储/物流/维修接入。下一步应针对失败类别修复或新增抽象 EvalCase，再重新跑同一版本套件；不能通过放宽比较器、删案例或把模型建议当修复。

## 7. 2026-09-08 Docker 恢复后的现场补测

本节是当前本机现场补测，使用 Compose 命名卷中的合成演示数据和运行期随机密码；密码、Token、订单号和原始响应没有写入证据文件。它不能把 deterministic manifest 注册数升级为完整现场通过数。

### 现场通过

| 场景 | 命令/路径 | 结果 |
| --- | --- | --- |
| 真实 Chrome 页面 | `tmp/run_browser_live.ps1` → `tmp/capture_demo_screenshots.py` | 退出码 `0`；客户、Agent、运营、质量四个真实页面截图已重新生成 |
| 权限/双账号归属 | `tmp/run_local_live_batch.ps1` → `verify_auth_flow.py` | 退出码 `0` |
| 统一售后 | 同上 → `verify_unified_after_sales_live.py` | 退出码 `0`；创建、确认、查询、取消、跨账号隔离通过 |
| Build 21 | 同上 → `verify_build21_authenticated_live.py` | 退出码 `0`；同会话服务重启后恢复通过（首次批次） |
| MCP | 同上 → `verify_mcp_authenticated_live.py` | 退出码 `0`；只读工具目录、主体隔离、SSE/会话边界通过 |
| 人工协同 | 同上 → `verify_service_case_live.py` | 退出码 `0`；入队、领取、补件、处理、结案和 B 账号隔离通过 |
| Java/MySQL Spring context | `mvn -pl mall-portal -am "-Dtest=MallPortalApplicationTests" "-DskipTests=false" "-Dsurefire.failIfNoSpecifiedTests=false" "-Dspring.config.additional-location=file:C:/Users/12969/Desktop/mall/tmp/maven-local-test.properties" test` | `Tests run: 1, Failures: 0, Errors: 0, Skipped: 0`；临时配置仅指向本地 Compose MySQL `3307`，未改正式配置 |
| Redis/RabbitMQ 重启恢复 | `docker compose restart redis`、`docker compose restart rabbitmq`，等待 health，再运行 `scripts/verify_compose_stack.py` | 两个容器重启命令退出码 `0`；等待后 8/8 healthy，endpoint `3/3` passed |

### 现场失败或波动

| 场景 | 实际结果 | 解释 |
| --- | --- | --- |
| Build 14A 退货状态脚本 | 退出码 `1`：Java 未创建 `return_refund` 申请 | 当前合成订单不满足 Java 退货资格；脚本已切换到当前统一售后 API，未使用旧接口伪造通过 |
| Build 21 独立重跑 | 两次退出码 `1`：`waiting diagnosis task is missing or malformed` | 真实模型/运行时路径出现非确定性；保留失败，不放宽断言。首次批次同脚本退出码 `0` 仍作为独立通过证据，不能写成稳定率 |
| 默认 Maven portal context | 退出码 `1`：`localhost:3306` 的 `Public Key Retrieval is not allowed` | 环境配置问题；使用不入库的临时本地连接配置重跑后 `1/1` 通过 |

### 仍未完成的清单

`browser_e2e` 24、`java_mysql_integration` 30、`fault_injection` 36、`durable_async_recovery` 32 是 manifest 中的完整逐条清单；本轮没有对应的 24/30/36/32 条独立现场执行器，因此仍分别记为 `environment_blocked`，不能用 manifest 478/478 或代表性检查替代。真实外部支付、仓储、物流、维修和生产 SLA 仍未接入/未验证。

本轮修改后的截图 SHA-256：

| 文件 | SHA-256 |
| --- | --- |
| `docs/assets/agent-task-workspace.png` | `cfa3e7a502d58fd02400a71285209aeb79db7bdbdcd37b5647b2be902ec8ac` |
| `docs/assets/customer-policy-conversation.png` | `56d5b8a645c4dbcf36a8b20d70c152175a0194f3dfaaa8b393832c4ff61b9b45` |
| `docs/assets/operations-handoff-overview.png` | `3676b9903c003847ee42962d24ae9c1d74630233ce4dae2b5486cdf41fb92ba1` |
| `docs/assets/quality-evaluation-dashboard.png` | `3ec40b0daf8b56df31a1d26f2799c188998e1ff631826ecbdbbcec28b49adaae` |

## 8. 2026-09-08 16:27 Docker 运行时恢复后的复验

最终补测前 Docker Desktop 曾因 Windows 上残留 AF_UNIX/reparse socket 无法启动；只将 Docker 内部运行时目录改名保留为可恢复备份，并确认 `EnableDockerAI=false`，没有 factory reset、镜像/卷/VHDX 删除或数据库清空。随后 Docker Engine `29.7.2` 恢复，Compose 服务重新拉起。

| 现场检查 | 命令/结果 |
| --- | --- |
| Docker 引擎 | `docker info --format '{{.ServerVersion}}'` → `29.7.2` |
| Compose 服务 | `docker compose ps` → 8/8 `healthy` |
| readiness | `scripts/verify_compose_stack.py` → exit `0`；Vue/FastAPI/Java endpoint `3/3` |
| 权限双账号 | `tmp/run_local_live_batch.ps1` → `verify_auth_flow.py` exit `0` |
| 统一售后 | 同上 → `verify_unified_after_sales_live.py` exit `0`；创建、确认、列表、状态、取消、跨账号隔离 |
| Build 21 | 同上 → `verify_build21_authenticated_live.py` exit `0`；同会话重启恢复 |
| MCP | 同上 → `verify_mcp_authenticated_live.py` exit `0` |
| 人工协同 | 同上 → `verify_service_case_live.py` exit `0` |
| Build 14A 退货状态 | exit `1`；Java 对当前合成订单按 `return_refund` 资格拒绝，未伪造通过 |

该批次使用运行期随机密码和现有合成演示数据；密码、Token、完整订单号和原始响应没有写入报告。此前 Build 21 两次独立重跑的等待任务缺失仍保留在第 7 节，故不能把单次成功写成稳定率。

`browser_e2e 24`、`java_mysql_integration 30`、`fault_injection 36`、`durable_async_recovery 32` 仍没有对应的逐条独立现场执行器，继续标为 `environment_blocked`；`478/478` 只代表 deterministic manifest 合同，不能替代这些现场套件。

本次证据同步提交 `73d12c5ecde9c56a21ce7be78e194d8fbc1e837c` 的 GitHub 门禁：[`mall-ci` run 34204868188](https://github.com/Eleven617/mall-ai-after-sales-platform/actions/runs/34204868188) 与 [`quality-evaluation` run 34204868215](https://github.com/Eleven617/mall-ai-after-sales-platform/actions/runs/34204868215) 均为 `success`。该提交只同步证据，不改变被测业务代码边界。

## 9. 2026-09-09 统一现场 Runner 收口复核

本节绑定当前代码提交 `45f842f9ed6c0a9b636e0420fe489312e71a28fb`。本次提交新增了数据驱动的四类现场 Runner、Build 14A 正/负资格验证器、真实 Chrome CDP 辅助器、PowerShell 入口和 Runner 合同测试；没有修改业务规则、Java 资格逻辑或数据库契约。

### 当前提交的确定性复验

| 范围 | 命令/结果 | 口径 |
| --- | --- | --- |
| FastAPI | `mall-ai-service\\.venv\\Scripts\\python.exe -m pytest -q` → **353 passed，7 subtests passed，1 warning，exit 0** | 本机确定性回归，不是真实模型准确率 |
| Runner 合同 | `pytest -q tests/test_field_acceptance.py tests/test_release_manifest.py tests/test_release_evaluation.py` → **11 passed，exit 0** | 只验证 Case 计数、状态模型和 Gate 判定 |
| Manifest/preflight | `validate_v3_release_manifest.py --json`、`run_v3_release_preflight.py --json` → **478/478、8/8，exit 0** | deterministic contract，不是 122 条现场通过 |
| Vue | `mall-ai-web\\npm run build` → **exit 0** | 类型检查与生产构建 |
| Java | portal **14/14**、admin **6/6**，均 `-DskipTests=false`、exit 0 | 定向单测；未把它写成完整 Java/MySQL 集成 |
| Compose | `docker compose config --quiet` → **exit 0** | 静态配置检查，不代表 daemon 健康 |

### 当前 Commit 的现场 Runner 结果

统一入口：`scripts\\Verify-FieldAcceptance.ps1` → `mall-ai-service/scripts/verify_field_acceptance.py`。本轮报告：

报告路径：`tmp/field-acceptance/field-20260909T074151Z-d56eb7fe/field-acceptance.json`

报告 SHA-256：`7da1d8efdd399d354ea535d32f9c24f9405508f5c57f9cb3364223219487e32a`
`testedCodeCommit`：`45f842f9ed6c0a9b636e0420fe489312e71a28fb`

| 类别 | Runner | 本轮执行 | 结果 |
| --- | ---: | ---: | --- |
| browser_e2e | 24/24 ready | 0 passed / 24 blocked | Docker daemon 启动前置失败 |
| java_mysql_integration | 30/30 ready | 0 passed / 30 blocked | Docker daemon 启动前置失败 |
| fault_injection | 36/36 ready | 0 passed / 36 blocked | Docker daemon 启动前置失败 |
| durable_async_recovery | 32/32 ready | 0 passed / 32 blocked | Docker daemon 启动前置失败 |
| 合计 | **122/122 ready** | **0 passed / 122 blocked** | **Release Gate 未通过** |

阻断摘要来自 Docker Desktop 后端日志：`sailor-ingest.sock` 无法重命名，Windows 返回 `error 1920 / The file cannot be accessed by the system`；本次报告同时记录 `docker_info` 失败、无 Fixture 绑定和重试入口。没有执行 factory reset、全局清理、卷/VHDX 删除或数据库清空。

### 旧现场报告的严格边界

以下报告确实曾在 2026-09-08 的 Docker/Chrome/Compose 环境中逐案运行，但其 `testedCodeCommit` 是 `314f5d9d3409cbdd8487d94a5a07ccbc705fd2a8`，不是当前 `45f842f`，所以在当前 Release Gate 中标为 **stale，不能和当前报告合并**：

| 报告 | 旧结果 | SHA-256 | 说明 |
| --- | --- | --- | --- |
| `field-20260908T100428Z-3865032c` | browser **24/24 passed** | `f77810192602c5a4936e2980f453066dd8657e8f72781c1b7cdf4070d75de6e8` | 真实 Chrome/CDP；旧 Commit |
| `field-20260908T100458Z-1f99435e` | Java/MySQL **30/30 passed** | `139f52b5cab07638f0a4c2641e9d2538030280fb540b4869e59a8081e6d6b7c4` | 真实 Compose Java/MySQL；旧 Commit |
| `field-20260908T100625Z-9e587842` | Durable **32/32 passed** | `4318fdea8f84909a13acdadc14ea91cf09af395282feb77a5fda708e6852d717` | 真实 Build 21 重启恢复；旧 Commit |
| `field-20260908T102006Z-f674a3bf` | Fault **36/36 passed**，但 Gate false | `5a3c9f2cfdd9d8ac3a48e92b5c64faad4be10574743363e111c94afb1ff4166e` | 隔离 Compose；Fixture 未绑定，故不能称完整 Gate 通过 |

这些旧报告证明 Runner 曾经可以在本机执行相应步骤，但不证明当前 Commit 的现场 Gate 已通过；当前环境恢复后必须重新执行同一入口并生成新的当前 SHA 报告。Build 14A 正/负路径也保留了此前一次通过记录，但当前 Commit 下尚未重新执行，不能扩展成当前 Release Gate 通过。

### 当前 Release Gate 结论

**未通过。** 当前提交的代码、确定性回归和 Runner 映射已经完成；122 条现场 Case 的当前提交重跑被 Docker Desktop 环境阻断，历史现场报告因 Commit 不一致标 stale。因而本项目现在可以如实写“已实现统一现场 Runner 并完成确定性门禁”，不能写“当前 Commit 的 122 条现场验收全部通过”。

## 10. 2026-09-09 Netty 安全修复与远程门禁复核

当前代码提交已更新为 `38601904595b6ae82a1e692d88c33d83d1ba1e01`。本次只修复依赖安全基线：`mall2/pom.xml` 将 `io.netty:netty.version` 从 `4.1.136.Final` 升级到 `4.1.137.Final`。此前 GitHub `dependency-and-secret-risk` 因 `netty-handler 4.1.136.Final` 命中 `GHSA-c4c3-7fpv-j4q5`（CVSS 9.1）和 `GHSA-fccg-mwvh-qqg4`（CVSS 6.9）失败；没有关闭扫描、放宽门禁或使用 `continue-on-error`。

### 当前提交的本机复验

| 范围 | 实际结果 |
| --- | --- |
| Java portal 定向测试 | `mvn -pl mall-portal -am ... -DskipTests=false test` → **14/14 passed**，exit `0` |
| Java admin 定向测试 | `mvn -pl mall-admin -am ... -DskipTests=false test` → **6/6 passed**，exit `0` |
| FastAPI 全量（代码改动无关） | **353 passed，7 subtests passed，1 warning**，exit `0` |
| Vue 构建（代码改动无关） | `npm run build` → exit `0` |
| Compose 静态配置 | `docker compose config --quiet` → exit `0` |
| 本地 OSV 复核 | 修复后报告 **0 packages affected / 0 known vulnerabilities**；该本地扫描器随后因未解析的本地 SNAPSHOT 模块退出 `127`，所以远程 Actions 才是本次门禁结论依据 |

### 当前提交的 GitHub Actions

| 工作流 | 运行 | 结果 |
| --- | --- | --- |
| `mall-ci` | [34333690241](https://github.com/Eleven617/mall-ai-after-sales-platform/actions/runs/34333690241) | **success**；Python、Java、Web、Compose contract、dependency-and-secret-risk 均通过 |
| `quality-evaluation` | [34333690290](https://github.com/Eleven617/mall-ai-after-sales-platform/actions/runs/34333690290) | **success** |

这两个运行绑定的是 `3860190`，因此可以证明当前代码提交的远程 CI 门禁通过。它们不能把 Docker 现场 Runner 的 `122 environment_blocked` 改写成现场通过。现场报告仍绑定 `45f842f`，与当前提交不一致，必须标记 **stale**；本次依赖升级后没有重新运行现场 Runner，因为 Docker Desktop 后端再次出现 `sailor-ingest.sock` 的 Windows `error 1920`，继续重试不会产生有效证据。

### 当前 Release Gate

**未通过。** 当前代码和远程 CI 已通过；确定性 manifest/preflight 仍为 `478/478`、`8/8`。但当前提交的 browser `24`、Java/MySQL `30`、fault `36`、durable recovery `32` 尚无新的逐条现场报告，不能宣称完整 `122/122` 现场通过。要完成现场 Gate，需要先修复 Docker Desktop/WSL 的内部 IPC 错误，再用当前提交和一次性合成 Fixture 重新执行 `scripts\\Verify-FieldAcceptance.ps1`。
