# v3.0.4 最小在线复测取证结论

更新时间：2026-09-21。该文档只汇总既有脱敏工件，不包含 Prompt、原始模型输出、reasoning、Secret 或业务标识，也没有触发新的外部模型调用。

## 批次身份与结论

- Release：`mall-v3.0.4-minimal-retest-78c7dd5-20260921`
- Batch：`minimal_retest-d551e9d01402`
- Runtime：`78c7dd5c0156d7d4b18df75b0df6221b6b2b4df4`
- 执行 HEAD：`a63d50d357296e5e9b1c9f23226992c9b6e8bf8d`
- Prompt / Skill Catalog：`agent_runtime_v3_4` / `skill_catalog_v3_1`
- 结论：批次真实启动，最终 `FAILED` / `NOT_COMPLETE`，不是“未启动”或 Provider 0。

Ledger 对账为 21 次逻辑请求、21 次 HTTP attempts、19 成功、2 失败，44,666 prompt tokens、29,167 completion tokens、总计 73,833 tokens。21 次 reservation 与 21 次 settlement 全部对应，无未结算 reservation、预算拒绝或自动重试。

## 执行范围

三条展示链的业务断言均通过：主售后闭环完成 Proposal 修订、Java 确认写入和回查，授权写入 1 次、重复写入 0、跨账号泄露 0；暂停恢复保持同一 Task 且无业务写入；事实变化路径拒绝旧 Proposal，旧 Proposal 写入 0。

两次失败均为单次 `network` Provider 事件，分别出现在暂停恢复和事实变化链的时间窗口。旧 Ledger 只记录 `operation=json`，没有模型角色和场景字段，因此不能从既有证据推断 executor、curator 或 critic。Runner 在展示结束后以 `provider_failure` 终止，11 个目标/成功对照 Case 全部 `not_executed`，不得写成模型评测通过或失败。

## 工件与完整性

- Report：`tmp/v304-minimal-retest-mall-v3.0.4-minimal-retest-78c7dd5-20260921/report.json`，SHA-256 `7793e790771003ea2e51375e1c899e6d2fd26176a4388f9a20d375b846b3305d`
- Ledger：`tmp/release-ledger-mall-v3.0.4-minimal-retest-78c7dd5-20260921/ledger.jsonl`，SHA-256 `44e2c38b186ed23a47d0bbc5596e36456c71a16dcfb612fc5ce22f1aee67777f`
- Lock：`docs/evidence/deepseek-release-lock-mall-v3.0.4-minimal-retest-78c7dd5-20260921.json`，SHA-256 `a359808bdab972dfda69e4b214eca437b0e5c1cc3ca53e3bf2789cd84a168a1e`

12 张 PNG 和 3 个 GIF 非空，但三组 evidence、handoff、status 帧分别具有相同哈希，不能证明三条独立场景发生了可见变化。原校验只检查单场景相邻帧，因而误判素材有效；这些素材不得用于最终作品集。

## 离线修复

Runtime `e1df8c4266f676330cb4c581a0c105dab10932a7` 为 Provider 事件增加受控角色和场景归因，展示链结束后发现 Provider 失败即停止，并拒绝跨场景复用同阶段帧。字段只允许安全枚举和计数，不保存 Prompt、输出或 reasoning。该修复不改变旧批次结论，也不把旧在线结果绑定到新 Runtime。
