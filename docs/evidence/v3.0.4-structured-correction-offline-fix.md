# v3.0.4 结构化输出离线修复证据

更新时间：2026-09-22。本文只使用已保存的脱敏诊断和离线 Fake Provider；没有读取或保存原始模型输出、reasoning、Prompt、凭据或业务标识，本轮外部 Provider requests/tokens 为 `0/0`。

## 历史失败边界

上一批次 `mall-v3.0.4-final-minimal-8ff255ae-20260921` / `minimal_retest-e61923d45dcb` 保持原样：16 HTTP attempts、15 成功、1 失败、49,158 tokens，reservation/settlement 16/16。`agent-open-010` 的失败发生在 Provider JSON 提取层，分类为 `model_contract_schema_invalid`。旧工件没有保存具体字段路径、原始响应或 `finish_reason`，因此无法确定历史内容属于缺字段、枚举、类型还是截断，也没有证据把它归因于本地输出长度上限。

## 根因、改动与预期

| 根因 | 实际改动 | 预期行为 | 离线回归 |
|---|---|---|---|
| 一次有界结构纠正已有实现，但安全上下文用只接受小写首字符的正则校验 `reference_hint_keys`；合法的 `orderRef` / `skuRef` 因 camelCase 被整段拒绝，Provider JSON 解析失败后无法进入纠正。 | `structured_output_gateway.py` 对引用键使用明确的 `orderRef` / `skuRef` 白名单；仍拒绝未知键和值，不回放客户原文。Prompt 协议提升为 `agent_runtime_v3_5`。 | 首次 malformed 输出仍记为失败；仅发送一次脱敏、无业务事实新增的结构纠正请求；二次失败继续 fail-closed。 | camelCase 正反例、首次失败+纠正双计账、严格 Schema、二次失败闭锁。 |
| malformed Provider 响应的 token usage 与 envelope 诊断在解析前后丢失。 | 在解析前读取 usage；只保留 `has_content`、`has_tool_calls`、allow-listed `finish_reason`、错误类别和字段路径；纠正请求写入 `protocolCorrection=true`。 | 首次失败和纠正分别进入 Ledger/预算，不泄漏响应正文或 reasoning。 | Fake HTTP 验证 7/11 tokens 分别记录，首次 failed、纠正 succeeded，纠正标记准确。 |
| 最小复测 Runner 将任意孤立 Provider 结构失败视为系统性故障，导致其余目标 Case 未收集。 | 仅由环境阻塞、账本/预算异常等硬停止条件提前终止；普通质量失败继续执行固定目标集合，目标未全过仍禁止进入展示阶段。 | 单个安全终止的 Schema 失败不掩盖剩余未知 Case，也不重试到通过。 | Runner 回归确认主阶段一个质量失败后仍执行 supplemental 与 Grounding，并跳过 showcase。 |
| 原入口只有单批预算，换 Release ID 可重置总量。 | 新增持久化 Campaign 守卫：最多 3 批、累计 240 attempts / 600,000 tokens；RUNNING、AMBIGUOUS、超额或对账不明均 fail-closed。 | 每批仍受 80/200,000 上限，已有更严格进程限制不会被放大；fake 统计不进入正式 Campaign。 | 三批累计、第四批拒绝、未结算/歧义拒绝、单批超额拒绝。 |

## 当前冻结验证

- Runtime/image：`62b61c2d64f75ffc80ed876976bc7d79e61c1d23`
- Prompt / Skill Catalog：`agent_runtime_v3_5` / `skill_catalog_v3_1`
- FastAPI JUnit：`494/494`（478 pytest cases + 16 subtests）
- Manifest / representative：`478/478` / `8/8`
- Contract replay：`36/36`，Provider calls/tokens `0/0`
- RAG 专项：`32/32`
- 现场：`122/122`，failed=0、environmentBlocked=0、Provider requests/tokens `0/0`
- 现场报告：`tmp/v304-field-acceptance-62b61c2/field-20260922T031548Z-a8e35576/field-acceptance.json`
- 现场报告 SHA-256：`51dff5a65ce8536089b4a4bbe0ecb1c52712a57f5d1c13fb72739ba6f0b24e54`

离线验证证明纠正机制、预算和阶段控制符合合同，不证明真实模型在目标 Case 上已经改善。新的在线目标集合仍为 `not_executed`，Release Gate 保持 `NOT_COMPLETE`。
