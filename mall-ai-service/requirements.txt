fastapi
uvicorn[standard]
pydantic
httpx
# Keep the IDNA parser on a patched, reproducible release.  An exact pin is
# intentional: OSV's manifest resolver otherwise selects the minimum version
# allowed by httpx and reports the vulnerable pre-3.15 line.
idna==3.19
python-dotenv
chromadb
redis>=5.0,<6
langgraph>=1.2,<2
fastembed==0.8.0
pymongo>=4.8,<5
