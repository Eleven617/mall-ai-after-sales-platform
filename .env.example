# Public Compose template.  It deliberately contains no credentials, tokens
# or model keys.  The Compose file supplies only its documented synthetic
# local-demo defaults; add real local values in an ignored `.env` file.
#
# For optional live model validation only, set DEEPSEEK_API_KEY in your local
# ignored `.env`.  Default CI and contract tests do not need a model key.
